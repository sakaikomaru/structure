name: structure

dependencies:
  - base >= 4.7 && < 5
  - array
  - bytestring
  - containers
  - deepseq
  - integer-gmp
  - mtl
  - stm
  - text
  - time
  - transformers
  - vector

library:
  source-dirs:
    - lib

executables:
  structure:
    main: Main.hs
    dependencies:
      - structure